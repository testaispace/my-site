# Next.js Web App Environment Configuration
# Copy this file to .env.local in this directory (apps/web/)
# DO NOT commit real secrets to version control

# ===========================================
# Supabase Configuration (Required)
# ===========================================
# Your Supabase project URL (public - safe for client-side)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous key (public - safe for client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase service role key (KEEP SECRET - server-side only)
# Used for admin operations, API routes, and bypassing RLS
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ===========================================
# Stripe Payment Configuration (Required)
# ===========================================
# Stripe publishable key (public - safe for client-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Stripe secret key (KEEP SECRET - server-side only)
# Used in API routes for payment processing
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe webhook secret (KEEP SECRET - for webhook verification)
# Used to verify webhook signatures from Stripe
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# ===========================================
# Mapbox Configuration (Required)
# ===========================================
# Mapbox public access token (public - safe for client-side)
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your_mapbox_public_token_here

# ===========================================
# App Configuration
# ===========================================
# Next.js environment
NODE_ENV=development

# App URL (used for callbacks, emails, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# Optional Configuration
# ===========================================
# JWT Secret for custom sessions (if not using Supabase auth entirely)
# JWT_SECRET=your_jwt_secret_here

# Redis URL for caching (if applicable)
# REDIS_URL=redis://localhost:6379

# Webhook URL for receiving Stripe webhooks
# WEBHOOK_URL=https://yourdomain.com/api/webhooks/stripe

# ===========================================
# Instructions for Web App
# ===========================================
# 1. Copy this file to .env.local in apps/web/ directory
# 2. .env.local is automatically ignored by git (Next.js convention)
# 3. For production, set these in your deployment platform (Vercel, etc.)
# 4. All NEXT_PUBLIC_ variables are exposed to the browser
# 5. Variables without NEXT_PUBLIC_ are server-side only
# 6. Restart your dev server after changing environment variables
