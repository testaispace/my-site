# Marketplace Monorepo Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit real secrets to version control

# ===========================================
# Supabase Configuration
# ===========================================
# Your Supabase project URL (public)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous key (public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase service role key (KEEP SECRET - server-side only)
# Used for admin operations and bypassing RLS
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ===========================================
# Stripe Payment Configuration
# ===========================================
# Stripe publishable key (public - for client-side)
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Stripe secret key (KEEP SECRET - server-side only)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe webhook secret (KEEP SECRET - for webhook verification)
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# ===========================================
# Mapbox Configuration
# ===========================================
# Mapbox public access token (public)
MAPBOX_PUBLIC_TOKEN=pk.your_mapbox_public_token_here

# ===========================================
# Expo Mobile App Configuration
# ===========================================
# Google Services JSON for Firebase (Android)
EXPO_GOOGLE_SERVICES_JSON=path/to/google-services.json

# Apple Provisioning Profile (iOS)
EXPO_APPLE_PROVISIONING_PROFILE=path/to/provisioning.mobileprovision

# ===========================================
# Additional Configuration (Optional)
# ===========================================
# App environment
NODE_ENV=development

# Database URL (if using direct connection)
# DATABASE_URL=postgresql://user:password@host:port/database

# Redis URL for caching (if applicable)
# REDIS_URL=redis://localhost:6379

# JWT Secret for custom auth (if applicable)
# JWT_SECRET=your_jwt_secret_here

# ===========================================
# Instructions
# ===========================================
# 1. Copy this file to .env in the root directory
# 2. Copy app-specific .env.example files to respective app directories
# 3. Fill in actual values (never commit these to git)
# 4. Ensure .env is in your .gitignore file
# 5. For production, use your platform's secret management system
